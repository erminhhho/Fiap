// Verificar se authorCount já foi declarado antes de criar
if (typeof authorCount === 'undefined') {
    let authorCount = 0;
    // Resto da lógica relacionada ao authorCount
} else {
    // Se já existir, use o authorCount existente
    console.log('authorCount já existe:', authorCount);
}

// Use const ou let para outras variáveis para evitar problemas de escopo
// Resto do código do arquivo...

// Remova declarações duplicadas de authorCount no resto do arquivo
